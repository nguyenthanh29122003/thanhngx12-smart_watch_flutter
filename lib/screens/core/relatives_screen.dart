// lib/screens/core/relatives_screen.dart
import 'package:flutter/material.dart';

class RelativesScreen extends StatelessWidget {
  const RelativesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Relatives')),
      body: const Center(child: Text('Relatives Screen - Placeholder')),
    );
  }
}
